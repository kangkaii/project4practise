package kk.practise.arraylist与数组;import java.util.ArrayList;import java.util.List;/** * @author kangkai on 2018/6/29. */public class Arraylist转数组 {    public static void main(String[] args) {        List<String> stockList = new ArrayList<String>();        stockList.add("stock1");        stockList.add("stock2");        String[] stockArr = new String[stockList.size()];        stockArr = stockList.toArray(stockArr);        //以上为list转成数组唯一方式；        //使用 下方 一步完成        String[] s02 = stockList.toArray(new String[stockList.size()]);        //函数式接口 IntFunction 的使用        String[] s03 = stockList.stream().toArray(String[]::new);        //下方代码会报错，即不能用于list - array//        Object[] stockArr = stockList.toArray();//        String[] stockArr01 = (String[])stockArr;        for(String s : stockArr)            System.out.println(s);    }}