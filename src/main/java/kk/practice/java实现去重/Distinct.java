package kk.practice.java实现去重;import org.junit.Test;import java.util.*;/**此类给 List<String> list * 去重，几种方法如下 * @author kangkai on 2018/4/12. */public class Distinct {    List<String> list = new ArrayList<>(){{        this.add("1");        this.add("1");        this.add("2");        this.add("3");    }};    @Test    public void disStringByList() {        //给 StringList 去重        List<String> result = new ArrayList<>(10);        //使用 result.contains(s) 判断        /*list.forEach(s -> {            if (result.contains(s)) {                return;            }else {                result.add(s);            }        });*/        //使用 result.stream().anyMatch() 判断        list.forEach(s -> {            boolean b = result.stream().anyMatch(r -> s.equals(r));            if (b) {                return;            }else {                result.add(s);            }        });        System.out.println(result);    }    @Test    public void disStringByHashSet() {        Set<String> result = new HashSet(list);        /*list.forEach(s -> {            result.add(s);        });*/        System.out.println(result);    }    @Test    public void disStringByHashMap() {        Map<String,String> result = new HashMap<>();        list.forEach(s -> {            result.putIfAbsent(s,"");        });        result.putIfAbsent("1","     ");        System.out.println(result);    }}